# Dev / docker-compose image for the Next.js frontend.
FROM node:20-alpine
WORKDIR /app

COPY package*.json ./
RUN npm ci

COPY . .

EXPOSE 3001
# -H 0.0.0.0 so the dev server is reachable through the published port.
CMD ["npm", "run", "dev", "--", "-H", "0.0.0.0", "-p", "3001"]
